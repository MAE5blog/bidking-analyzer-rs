# AuctionAnalyzer 4.12.2 notes

Local source executable:

```text
C:\Users\Administrator\Downloads\AuctionAnalyzer4.12.2\AuctionAnalyzer4.12.2.exe
```

Local extraction output:

```text
D:\workdir3\bidking\extracted_4_12_2
D:\workdir3\bidking\decompiled_4_12_2
```

Observed changes against 4.12.1:

- `MapBidCalculator.dll` changed from 907,776 bytes to 937,472 bytes.
- `MapBidCalculator.calculator_data_merged.csv` changed from 9,181 lines to
  9,593 lines. The added records are extra `DROP` entries.
- `MapBidCalculator.drop_table_weights.csv` is unchanged.
- `MapBidCalculator.item_prices.csv` is unchanged.
- `DataLoaderService.cs` is unchanged.
- `MainWindow.cs` changed around color average validation and high-quality
  item value blending.

Implementation decisions in this Rust project:

- The Rust code is a clean-room reimplementation driven by observed behavior
  and local reference tests.
- Original DLLs, decompiled C# files, and bundled executable resources are not
  embedded in the Rust source tree.
- The CLI loads external CSV/JSON data paths. A future public release should
  provide an importer or require users to supply their own data export.

