# AuctionAnalyzer 4.12.3 notes

Local source executable:

```text
C:\Users\Administrator\Downloads\AuctionAnalyzer4.12.3.exe
```

Local extraction output:

```text
D:\workdir3\bidking\extracted_4_12_3
D:\workdir3\bidking\decompiled_4_12_3
```

Observed changes against 4.12.2:

- `MapBidCalculator.dll` changed from 937,472 bytes to 939,008 bytes.
- `MapBidCalculator.calculator_data_merged.csv` is unchanged.
- `MapBidCalculator.drop_table_weights.csv` is unchanged.
- `MapBidCalculator.item_prices.csv` is unchanged.
- `mainwindow.baml` and `startupnoticewindow.baml` are unchanged.
- `OcrParser.cs`, model classes, and embedded static map data are unchanged.
- `AutoOcrService.cs` changed OCR queue initialization and explicit disposal paths.
- `MainWindow.cs` added a real item-grid reachability check using valid item grid
  sizes: `1,2,3,4,5,6,8,9,10,12,15,16,18`.
- `MainWindow.cs` also normalizes zero count/grid/average triads when zero is
  treated as missing.

Implementation decisions in this Rust project:

- Keep the embedded data version at `auctionanalyzer-4.12.3`.
- Copy the unchanged 4.12.3 merged CSV into `data/auctionanalyzer-4.12.3`.
- Sync the 4.12.3 grid reachability validation into the Rust core.
- Keep OCR on the existing ONNX Runtime + PP-OCRv4 path; the original app's
  PaddleOCR queue/disposal changes do not map directly to this implementation.
